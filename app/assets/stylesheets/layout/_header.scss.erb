@import 'base/typography';

.page-header {
  background-image: url(<%= asset_path 'mask_group_4' %>);
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  align-items: center;
  display: flex;
  justify-content: center;
  height: 60px;

  .content {
    font-size: 20px;
    color: $font-white;
  }
}
